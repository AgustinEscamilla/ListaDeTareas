import { createContext } from 'react'

// Maestro use aqui este archivo solo para crear el contexto
// Esto es asi para cumplir la regla de una cosa por archivo
export const ContextoTema = createContext()